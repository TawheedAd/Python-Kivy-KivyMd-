maincode:

<maincode>:
    orientation:"vertical"
    TextInput:
        id:my_text_input
        text:"hello"
        multiline: False
        on_text_validate:root.on_text_validate(self)
    Label:
        text:root.text_input_str