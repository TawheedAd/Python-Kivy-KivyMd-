WidgetExample:
<WidgetExample>:
    cols:3
    ToggleButton:
        text:"OFF"
        on_state: root.on_toggle_button_state(self)
        size_hint:None,1
        width:"100dp"
    Button:
        text:"Count"
        on_press: root.on_button_click()
        disabled:not root.counter_enable
    Label:
        text:root.my_text
        font_name:"fonts/lcd.ttf"
        font_size:"80dp"
        color:1,.5,1,1
    Switch:
        id:my_switch
        size_hint:None,1
        width:"100dp"
        on_active:root.on_switch_active(self) #self is switch itself
        active:True
    Slider:
        id:my_slider
        min:0
        max:100
        value:50
        disabled:not my_switch.active
    BoxLayout:
        orientation: "vertical"
        Label:
            text:str(int(my_slider.value))
            font_name:"fonts/lcd.ttf"
            font_size:"80dp"
            color:1,.5,1,1
        ProgressBar:
            max:100
            value:my_slider.value

